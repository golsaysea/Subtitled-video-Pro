[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Subtitled-video-Pro"
version = "1.0.2"
description = "A professional video subtitling tool."
dependencies = [] # 如果你有依赖包，写在这里
