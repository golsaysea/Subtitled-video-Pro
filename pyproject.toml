[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Subtitled-video-Pro"
version = "1.0.4"
description = "Professional video subtitling tool"
dependencies = [
    "PyQt6",
    "requests",
    "playwright"
]
